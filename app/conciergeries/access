'use client';

import React, { useState } from 'react';
import Link from 'next/link';
import { Button, Input, Textarea, Reveal, Marker, Label } from '../../../components/ui';

export default function ConciergeAccessPage() {
  const [form, setForm] = useState({
    companyName: '',
    fullName: '',
    email: '',
    phone: '',
    destinations: '',
    volume: '',
    message: '',
  });

  const onSubmit = async (e: React.FormEvent) => {
    e.preventDefault();

    // TODO: branche ici ton endpoint (Resend / Formspree / API route / submitRequest dédié)
    console.log('Concierge access request:', form);

    alert('Merci ! Votre demande a bien été envoyée.');
  };

  return (
    <div className="min-h-screen bg-[#FDFCFB] pt-32 pb-24 px-6 md:px-12">
      <div className="max-w-3xl mx-auto">
        <Reveal>
          <Marker className="mb-8 bg-stone-900" />
          <h1 className="text-4xl md:text-5xl font-serif text-stone-900 mb-4">
            Demander l’accès à la Sélection Select
          </h1>
          <p className="text-stone-500 font-light leading-relaxed mb-12">
            Présentez-nous votre conciergerie. Nous reviendrons vers vous pour valider l’accès et activer la mise en relation.
          </p>

          <form onSubmit={onSubmit} className="space-y-10">
            <div className="space-y-4">
              <Label>Nom de la conciergerie</Label>
              <Input
                value={form.companyName}
                onChange={(e) => setForm({ ...form, companyName: e.target.value })}
                placeholder="Ex: Alpine Privilege"
                required
              />
            </div>

            <div className="grid md:grid-cols-2 gap-8">
              <div className="space-y-4">
                <Label>Nom complet</Label>
                <Input
                  value={form.fullName}
                  onChange={(e) => setForm({ ...form, fullName: e.target.value })}
                  placeholder="Prénom Nom"
                  required
                />
              </div>
              <div className="space-y-4">
                <Label>Email</Label>
                <Input
                  type="email"
                  value={form.email}
                  onChange={(e) => setForm({ ...form, email: e.target.value })}
                  placeholder="email@conciergerie.com"
                  required
                />
              </div>
            </div>

            <div className="grid md:grid-cols-2 gap-8">
              <div className="space-y-4">
                <Label>Téléphone</Label>
                <Input
                  type="tel"
                  value={form.phone}
                  onChange={(e) => setForm({ ...form, phone: e.target.value })}
                  placeholder="+33 ..."
                />
              </div>
              <div className="space-y-4">
                <Label>Destinations</Label>
                <Input
                  value={form.destinations}
                  onChange={(e) => setForm({ ...form, destinations: e.target.value })}
                  placeholder="Courchevel, Ibiza, Monaco..."
                />
              </div>
            </div>

            <div className="grid md:grid-cols-2 gap-8">
              <div className="space-y-4">
                <Label>Volume estimé</Label>
                <Input
                  value={form.volume}
                  onChange={(e) => setForm({ ...form, volume: e.target.value })}
                  placeholder="Ex: 5-10 missions / mois"
                />
              </div>
              <div className="space-y-4">
                <Label>Type de missions</Label>
                <Input
                  value={form.message}
                  onChange={(e) => setForm({ ...form, message: e.target.value })}
                  placeholder="Villas, chalets, yachts, résidences..."
                />
              </div>
            </div>

            <div className="space-y-4">
              <Label>Message</Label>
              <Textarea
                value={form.message}
                onChange={(e) => setForm({ ...form, message: e.target.value })}
                placeholder="Décrivez vos standards et votre besoin."
                className="min-h-[140px]"
              />
            </div>

            <div className="flex items-center justify-between pt-6 border-t border-stone-100">
              <Link href="/conciergeries" className="text-sm text-stone-400 hover:text-stone-900">
                ← Retour
              </Link>
              <Button type="submit" className="w-64">
                Envoyer la demande
              </Button>
            </div>
          </form>
        </Reveal>
      </div>
    </div>
  );
}
